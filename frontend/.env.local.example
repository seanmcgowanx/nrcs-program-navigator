# Copy to .env.local for local development.
# Base URL of the FastAPI backend (nrcs_navigator.serving.app).
# Local dev points at uvicorn; in production set this to the Render URL.
NEXT_PUBLIC_API_URL=http://localhost:8000
