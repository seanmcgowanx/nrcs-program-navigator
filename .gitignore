# --- Secrets ---
.env

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/

# --- Virtual environments ---
.venv/
venv/

# --- Poetry (keep poetry.lock committed; ignore nothing else here) ---

# --- Data artifacts (downloaded CSV + eCFR PDFs; regenerated by the pipeline) ---
# Keep the directory structure (.gitkeep) but ignore the contents.
# Cleaned payments and embeddings live in Postgres, not on disk.
data/raw/*
!data/raw/.gitkeep

# --- OS / editor ---
.DS_Store
.idea/
.vscode/
